// COMPILE-ONLY STUB. Mirrors the member of Create 6.0.10's class that Create IceCream uses.
// It is NOT packaged: at runtime the real class from Create is used.
package com.simibubi.create.content.trains.track;

import com.mojang.blaze3d.vertex.PoseStack;
import com.mojang.blaze3d.vertex.VertexConsumer;

import net.minecraft.world.level.Level;

public class TrackRenderer {
    public static void renderBezierTurn(Level level, BezierConnection bc, PoseStack ms, VertexConsumer vb) {
        throw new AssertionError("stub");
    }
}
