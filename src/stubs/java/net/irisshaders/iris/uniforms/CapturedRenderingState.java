// COMPILE-ONLY STUB. Mirrors the public signature of the real class so Create IceCream can be compiled
// without the mod jar. It is NOT packaged: at runtime the real class from Iris is used.
package net.irisshaders.iris.uniforms;

public class CapturedRenderingState {
    public static final CapturedRenderingState INSTANCE = new CapturedRenderingState();

    public int getCurrentRenderedBlockEntity() { throw new AssertionError("stub"); }
    public int getCurrentRenderedEntity() { throw new AssertionError("stub"); }
    public int getCurrentRenderedItem() { throw new AssertionError("stub"); }
}
